---
import { Download, Code } from "lucide-astro";
---

<section class="relative overflow-hidden py-16 md:py-24 lg:py-32">
  <div class="organic-blob" style="background: var(--sage); width: 500px; height: 500px; top: -10%; right: 10%;"></div>
  <div class="organic-blob" style="background: var(--terracotta); width: 400px; height: 400px; bottom: -5%; left: 5%; animation-delay: -5s;"></div>

  <!-- Botanical decorative leaf -->
  <div class="botanical-leaf" style="color: var(--accent-yellow); width: 120px; height: 120px; top: 10%; left: -3%; transform: rotate(-25deg) scaleX(-1);"></div>
  <div class="botanical-leaf" style="color: var(--sage); width: 80px; height: 80px; top: 18%; left: 2%; transform: rotate(15deg); opacity: 0.4;"></div>

  <div class="relative mx-auto max-w-6xl px-6">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 md:gap-10 items-center">
      <div class="lg:col-span-7">
        <div class="fade-in" style="animation-delay: 0.1s;">
          <div class="inline-block mb-3 px-4 py-1.5 bg-terracotta text-cream text-sm font-display font-semibold tracking-wide" style="transform: rotate(-1deg);">
            BETA
          </div>
        </div>

        <h1 class="font-serif text-6xl md:text-7xl lg:text-8xl leading-[0.95] text-brown fade-in" style="animation-delay: 0.2s;">
          For wandering<br/>
          <span class="italic text-sage">minds</span> and<br/>
          growing <span class="text-terracotta">ideas</span>
        </h1>

        <p class="mt-6 text-xl md:text-2xl text-muted max-w-xl leading-relaxed fade-in" style="animation-delay: 0.3s;">
          Ideas are seeds — stop holding them, start planting them in your digital garden.
        </p>

        <div class="mt-8 flex flex-col sm:flex-row items-start gap-4 fade-in" style="animation-delay: 0.4s;">
          <a href="#download" class="btn-primary flex items-center gap-3">
            <Download size={22} />
            <span>Download for Mac</span>
          </a>
          <a
            href="https://github.com/nicholasgriffintn/holophyte"
            target="_blank"
            rel="noopener noreferrer"
            class="btn-secondary flex items-center gap-3"
          >
            <Code size={22} />
            <span>View Source</span>
          </a>
        </div>
      </div>

      <div class="lg:col-span-5 fade-in" style="animation-delay: 0.5s;">
        <div class="relative">
          <div class="bg-cream-dark p-8 border-4 border-brown" style="transform: rotate(1deg);">
            <div class="font-display text-brown text-sm font-semibold mb-4 tracking-wide">HOLOPHYTE</div>
            <div class="space-y-3">
              <div class="h-2 bg-sage w-3/4"></div>
              <div class="h-2 bg-brown-light w-full"></div>
              <div class="h-2 bg-terracotta w-1/2"></div>
              <div class="mt-6 pt-4 border-t-2 border-brown/20">
                <div class="h-2 bg-sage w-2/3 mb-3"></div>
                <div class="h-2 bg-brown-light w-5/6"></div>
              </div>
            </div>
          </div>
          <div class="absolute -bottom-4 -right-4 w-full h-full border-4 border-terracotta -z-10"></div>
        </div>
      </div>
    </div>
  </div>
</section>
